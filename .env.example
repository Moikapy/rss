# Auto-generated on first run — DO NOT commit this file

# JWT secret for auth tokens (required)
JWT_SECRET=

# Ollama (local LLM for AI chat) — used in local dev only
OLLAMA_HOST=http://localhost:11434
OLLAMA_CHAT_MODEL=llama3.2
OLLAMA_EMBED_MODEL=embeddinggemma

# API Worker URL (leave empty for relative paths in dev)
# In production, set to the API Worker URL
NEXT_PUBLIC_API_URL=